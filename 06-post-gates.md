# 成品验证与修缮入口

**位置**：汤面/汤底写完后、输出前。所有汤必跑。

**原则**：能被前移到创作阶段的约束已由解题链卡（02）和状态协议卡（04）承担。本文件仅保留无法前移的事后验证。

**失败后不是立刻补句子。** 先打开 [appendix/e-repair-diagnosis.md](appendix/e-repair-diagnosis.md)，判断问题是锚点、接力、机制、双读还是故事本体；再按“语义影响”重跑最小验证集。

---

## 闸门总览

| # | 闸门 | 类型 | 说明 |
|---|------|------|------|
| G1a | 表层自洽 | 结构 | 遮住汤底，汤面在该载体的表层契约中自洽、可阅读、且给出公平的问题空间？ |
| G1b | 真相自洽 | 结构 | 遮住汤面，汤底是否是完整的因果说明/文档解释/拼图复原/规则模型？ |
| G1c | 承重覆盖 | 结构 | 每个承重汤面事实在汤底中有明确位置 + 每个隐藏变量有L1足迹（反向"无孤儿"检查） |
| G2 | 载体约束点双模型审计 | 加密 | 按载体审计P和T在约束点上的各自成立性（A-E各表） |
| G3 | 排除无辜解释 | 指纹 | 题面事实能否排除"没有超自然因素"？ |
| G4 | 锁定变格机制 | 指纹 | 能否排除其他变格解释？ |
| G5 | 情感落点验证 | 体验 | 汤底情感在汤面中有行为锚点吗？ |
| G6 | 零外部知识盲测 | 可达 | 子代理闭卷测试 |
| G7 | 开局三问+老手套模板 | 距离 | 标准清单穿透测试 |
| G8 | 全量双模型审计 | 加密 | 按载体分流——A保留逐句双读；D改为碎片关系审计；B/C/E补专属审计 |
| G9 | 模拟问答 | 可达 | 15问对照解题链卡验证 |
| G10 | 解题链卡完整性格 | 元闸门 | 解题链卡所有字段非空？ |
| G11 | 推理保证闸门 | 可推演 | 匿名模拟硬检测穷举点；每步信念更新有逻辑落脚点？ |

---

## G1a·表层自洽

遮住深层模型 T，只看表层模型 P（汤面）——它在当前载体的表层契约中是否自洽、可阅读、且给出公平的问题空间？

- ✓ 汤面在该载体形态下自洽可读，玩家能从汤面中形成明确的初始理解和自然的第一问
- ✗ 不自洽 → 回第一步重修表层模型 P

**载体差异化**：
- A事件汤：汤面是否是一个完整的"外部观察者能记录"的叙事？
- B感官汤：感官描述是否在"日常来源"框架下自然成立？
- C文档汤：文档在"作者正常写作"假设下是否自洽（即使有异常）？
- D拼图汤：每块碎片是否局部诚实、可独立理解？（不要求"完整叙事"）
- E机制汤：现象描述是否在"无隐藏规则"视角下成立？（不要求"故事"）

## G1b·真相自洽

遮住表层模型 P（汤面），只看深层模型 T（汤底）——它是否是完整的因果说明/文档解释/拼图复原/规则模型？

- ✓ T 有完整的因果链或解释模型——不依赖汤面就能读懂
- ✗ T 是"对汤面每句话的隐喻翻译"→ 回第三步（P-T咬合）

**不强制是"独立短篇小说"**：C文档汤的 T 可以是文档真实创作条件的说明；D拼图汤的 T 可以是碎片合成后的事件真相；E机制汤的 T 可以是规则的完整运作模型。只要遮住汤面仍能独立读懂——通过。

## G1c·承重覆盖（反向"无孤儿"检查）

正向：每个承重汤面事实在 T 中有明确的位置——它能被 T 解释。

反向：每个通关所需隐藏变量都在汤面或前序确认事实中留有 L1 足迹——不存在"汤底有、但汤面完全找不到痕迹"的孤儿变量。

- ✓ 正向和反向全部覆盖
- ✗ 正向有汤面事实在 T 中无解释 → P 和 T 没有复原同一个 U，回第三步
- ✗ 反向有变量无 L1 足迹且不在链式抵达豁免中 → 回第二步乙补锚点

---

## G2·载体约束点双模型审计

按载体类型，审计表层模型 P 和深层模型 T 在约束点上是否各自成立：

| 载体 | 审计对象 | 通过条件 |
|------|---------|---------|
| **A** | 承重行为的表层因果与深层因果 | 同一可观察行为在 P 中是无辜动机、在 T 中是暗线动机，两者各自在自己框架中成立 |
| **B** | 感官描述诚实性 + 来源可区分性 | 汤面描述在 P 的日常来源下自然成立；两种来源物理可混淆；存在至少一种可观察区分手段 |
| **C** | 字面语义、格式、来源、时间和措辞 | 文档在"正常写作"假设下表面自洽；格式异常能从表层材料逆向锁定至少一个真相约束 |
| **D** | 碎片局部诚实 + 碎片关系唯一合成 | 每块碎片自身诚实（不故意误导）；碎片关系（非单块双义）能唯一合成事件真相 |
| **E** | 现象与隐藏规则的一致性 + 预测区分 | 所有汤面现象在隐藏规则下有唯一解释；至少一个现象能排除至少一种替代规则 |

任一否 → 回第二步（定位载体约束点）。

---

## G3·排除无辜解释（变格专用，本格跳过）

题面事实能否排除"这事根本没有超自然因素"的解释？

- 能 → 通过
- 不能 → 变格指纹不够利。回第一步（故事独立生长）

---

## G4·锁定变格机制（变格专用，本格跳过）

把汤底机制替换成另一种变格解释→汤面的每一句话在替换后是否仍然成立？

- 全部成立 → 指纹不够利。回第一步
- 至少有一个表面事实能把本题的变格规则与其他替代解释区分开 → 通过

---

## G5·情感落点验证

委托卡中的"终点认知句"——汤底揭晓后，玩家能否在脑中自行说出这句话？

- 能 → 通过
- 不能（需要主持人补充情感旁白才能感受到）→ 汤底情感用途缺少行为锚点。回第一步补行为锚点

---

## G6·零外部知识盲测（子代理闭卷）

派遣一个**不知汤底的子代理**作为盲测玩家。子代理只拿到汤面、载体类型、世界规则、内容边界。

### 盲测输入

```
你是海龟汤盲测员。以下是你会看到的信息——和真实玩家开局时完全一致：

【汤面】
[汤面全文]

【载体类型】[A-E]
【世界规则】[本格/微变格/变格]
【内容边界】[有/无 死亡/血腥/惊吓/敏感议题]

你的任务：
1. 读完汤面后，输出你最先会问的10个具体"是不是"问题。
   每个问题附带一句话说明：你为什么会想到问这个（引用汤面中哪个具体细节触发了它）。
   问题必须按你会实际问的顺序排列。

2. 指出你读完汤面后第一个注意到的"接缝"——那个让你觉得"这里有问题"的具体细节。

3. 识别汤型模板：这碗汤让你想到哪个已知海龟汤类型？
   输出你脑中闪过的"最可能的答案框架"（1-2句话）。

4. 如果允许你继续追问（在收到前10问的答案后），你预计还需要多少问才能确定核心真相？
   不需要精确——给出范围即可（如"10-20问""30+问"）。
```

### 盲测对照分析

**A段·命中检查**（对照汤底，逐条标记）：

| 问号 | 问题 | 命中等级 | 判定 |
|------|------|---------|------|
| q1 | ... | 方向错误 / 擦边 / 接近核心 / 命中公理 | |
| ... | ... | | |

**B段·框架分析**：子代理识别到的"汤型模板"和"答案框架"是否包含正确方向？
- 包含 → 框架预载泄露。记录为加密脆弱点。
- 不包含 → 框架伪装有效。

**C段·难度对照**：

| 目标难度 | 盲测准入线 | 实际结果 | 判定 |
|---------|-----------|---------|------|
| E | q1可命中方向 | | |
| D | q1不可命中方向 | | |
| C | 前5问不可命中方向，前10问不可命中公理 | | |
| B | 前10问不可命中方向 | | |
| A+ | 前10问不可命中方向，前15问不可命中公理 | | |

不通过 → 回第四步或第二步（汤面/接缝或交叉点）。

---

## G7·开局三问+老手套模板

### 开局三问防护

| # | 问题 | 答案是否直接指向核心方向？ |
|---|------|------------------------|
| 1 | 有死人吗？ | |
| 2 | 有超自然力量/变格因素吗？ | |
| 3 | 主角/叙述者是人类吗？ | |

任一答案直接指向被加密的公理 → 距离失败。

### 标准清单20问穿透测试

逐一遍历以下20问。对每一问，判断：如果玩家在第N问问出这一题，答案是否直接指向核心方向？

| # | 问题模板 | 适用条件 | 命中？ |
|---|---------|---------|--------|
| 1 | 有人死了吗？ | 通用 | |
| 2 | 有超自然力量/变格因素吗？ | 通用 | |
| 3 | 主角/叙述者是人类吗？ | 通用 | |
| 4 | 叙述者就是死者吗？ | 含叙述者 | |
| 5 | X和Y是同一个人吗？ | 含多角色 | |
| 6 | X是Y伪装的吗？ | 含多角色 | |
| 7 | X是真实存在的还是幻觉/虚构？ | 含角色 | |
| 8 | 孩子是亲生的吗？ | 含亲子关系 | |
| 9 | X出轨了吗？ | 含配偶/伴侣 | |
| 10 | X有精神/心理问题吗？ | 含异常行为 | |
| 11 | X在说谎/隐瞒吗？ | 含陈述/口供 | |
| 12 | 这件事发生在X年前/另一个时间？ | 含时间线索 | |
| 13 | X和Y有血缘关系吗？ | 含多角色 | |
| 14 | X已经死了但别人不知道？ | 含失踪/异常 | |
| 15 | X杀死/伤害了Y吗？ | 含冲突 | |
| 16 | X在保护/掩盖Y吗？ | 含包庇/隐瞒 | |
| 17 | X知道自己做的事意味着什么吗？ | 含无知/误解 | |
| 18 | 有双胞胎/替身/复制体吗？ | 含相似角色 | |
| 19 | 看到的/听到的是另一种东西吗？ | B类通用 | |
| 20 | 规则/系统的真正目的和表面不同？ | C/E类通用 | |

**判定**：任一标准问题在目标难度准入线内命中核心方向 → 不通过。命中数量≥准入线的20% → 不通过。

不通过 → 回第四步或第二步。

---

## G8·全量双模型审计（按载体分流）

不设抽样，不设豁免。按载体类型执行不同审计：

### A·事件汤 — 逐句双读（左列无辜·右列汤底）

汤面每一句话，左列无辜、右列汤底，两列独立成立。

| 失败模式 | 判据 | 修 |
|---------|------|----|
| 左列为空 | 写不出无辜解释 | 删或重写 |
| 左列指向真相 | 无辜成立但第一念头恰好是汤底方向 | 换措辞 |
| 左右相同 | 无辜和汤底没区别 | 删 |
| 左列不坚固 | 靠"可能""也许"才成立 | 重写 |

### D·拼图汤 — 碎片关系审计

不逐句双读。审计每块碎片的局部诚实性和碎片间关系的合成唯一性。

| 失败模式 | 判据 | 修 |
|---------|------|----|
| 单块碎片不诚实 | 碎片内部信息在该碎片视角下是假的 | 重写碎片 |
| 碎片关系无信息差 | 两块碎片的关系可用"不同人记性不同"消解 | 加强碎片矛盾 |
| 碎片关系不可逆 | 从碎片关系能推出多种合成方式 | 增加交叉锁定 |

### B·感官汤 — 感官描述审计

审计每个感官描述词在两种来源下的自然度和可区分性。

| 失败模式 | 判据 | 修 |
|---------|------|----|
| 描述词在真实来源下不自然 | 正常人看到真实来源不会用汤面那个词 | 换描述词 |
| 两种来源物理不可混淆 | 正常人不可能误认 | 调整来源选择 |
| 区分手段不存在 | 玩家无法通过可观察手段区分 | 补区分锚点 |

### C·文档汤 — 语义/格式审计

审计文档异常的"正常写作"兼容性和逆推唯一性。

| 失败模式 | 判据 | 修 |
|---------|------|----|
| 异常用"粗心"可完全消解 | 所有异常都可用作者不小心解释 | 加强异常系统性 |
| 异常只指向一种解释 | 看到异常=知道真相（太薄） | 加竞争解释 |
| 格式异常不可逆 | 从异常能猜真相，但从真相不能验证异常 | 补反向验证 |

### E·机制汤 — 现象/规则审计

审计现象模式对隐藏规则的归纳支持度。

| 失败模式 | 判据 | 修 |
|---------|------|----|
| 现象不排除随机 | 所有现象可用"巧合"解释 | 增加现象系统模式 |
| 缺少预测型现象 | 所有现象只能回看 | 补预测/反例型现象 |
| 替代规则未被排除 | 另一种规则也能解释所有现象 | 补排除性现象 |

任一命中 → 回第四步（汤面措辞）。

---

## G9·模拟问答（对照解题链卡验证）

列出15个玩家高频问题及预期答案。然后对照解题链卡做双向验证：

1. **覆盖验证**：解题链卡中每个"答案后新方向"是否在模拟问答中至少出现了一次？没出现 → 那个方向是作者一厢情愿。
2. **收敛验证**：最后3问应呈现收敛趋势（问题越来越具体，指向核心事实而非新领域）。
3. **烟雾弹验证**：前5问中至少2问被引向非核心方向——否则烟雾弹无效。
4. **连续"是"验证**：不允许连续3问的答案全是"是"但玩家不知道下一步问什么。

---

## G10·解题链卡完整性格（元闸门）

```
解题链卡所有字段非空？：
├─ 入口锚点：接缝类型 + 首轮方向≥2 + 入口安全性 → [ ] 全部填写
├─ 拍间发现链：逐拍答案 + 新方向≥2 + 承重事实 + 锚点来源 + 触发源 → [ ] 全部填写
├─ 可达性审计：逐事实逐来源 → [ ] 全部填写
├─ 可导航性审计：逐拍方向+通向 → [ ] 全部填写
└─ 可证实性审计（机制/状态型）：检验+预期结果 → [ ] 不适用 / 全部填写
```

任一字段为空 → 解题链卡未完成。补全后再跑后续闸门。

---

## G11·推理保证闸门（匿名模拟）

### G11 前置·反事实足迹回溯（L2+L3 终验）

在跑推理审计三问之前，先过这道前置。复用步骤 2乙（足迹预检）的变量清单。

**对每个隐藏变量 V**：列出至少两个竞争解释 H'₁、H'₂，逐条确认足迹是否实际排除了它们。

```
变量 V：______（类型：___）

竞争解释 H'₁：______
  → 与汤面/前序事实 F 中哪个细节矛盾？：[具体细节 / ⚠ 无矛盾]
竞争解释 H'₂：______
  → 与汤面/前序事实 F 中哪个细节矛盾？：[具体细节 / ⚠ 无矛盾]
```

- 任一 H' 与 F 全兼容 → ⚠ 该竞争解释未被排除。标记为区分性不足。
- 所有 H' 都与 F 中至少一个事实矛盾 → L2 通过。

**L3·结论收敛**：所有变量合在一起，是否唯一解释 F 中的全部事实？
- 是 → 收敛。通过。
- 否 → 存在完整替代假说与 F 全兼容 → 穷举风险，回步骤 2乙或步骤 4。

### 操作流程

```
对每一拍：
1. 列出该拍开始时玩家确切知道的事实集合F（汤面事实 + 前序已确认事实）
2. 写出正确假设H（该拍要达成的信念更新）
3. 列出所有替代假设H'（至少2个，这些是玩家可能产生的"竞争假说"）
4. 逐一检查每个H'是否与F中至少一个事实矛盾
   → 若存在H'与F完全一致 → 穷举点 → 标记，回步骤 4（重新设计原子/接缝）
   → 若所有H'都与F中至少一个事实矛盾 → 通过。玩家有足够信息用推理排除错误假设
```

### 推理审计三问（快速版，逐信念更新）

| 问 | 检查 | 不通过含义 |
|---|---|---|
| Q1 方向问 | 玩家此时的信息能否将假设空间缩小到可枚举（≤5）？ | 穷举：方向无限，乱试 |
| Q2 区分问 | 可枚举假设中，已有信息能否唯一确定正确假设？ | 穷举：多假设兼容，猜或逐一排除 |
| Q3 锁定问 | 确认正确假设后，能否用已有事实自证（不依赖主持人确认）？ | 穷举：靠"主持人说是"才确认 |

三问全部通过 → 该步有推理落脚点。任一不通过 → 穷举风险，进入修缮诊断。

### 独立证据族终验

对含 [关系/事件/规则] 标签的变量，回看整条解题链——≥2 个独立证据族是否分散在不同的信息渠道中？

独立族判定见 appendix/f。两个追问同一条扼痕的细节 → 不独立。档案 vs 物理痕迹 → 独立。规则变量至少一个证据族是预测/反例型（非仅回看已有异象）。

- 独立族 <2 → 区分性不足。回步骤 2乙或步骤 4。
- 独立族 ≥2 且渠道分散 → 通过。

### D级追加检查

- **独立可验证**：单独抽出任何一拍，该拍的汤面+前置信息仍能支撑该拍的推理跳跃
- **链断裂检测**：在某拍猜错方向时，前序各拍的信息能否把玩家拉回来？不能 → 该拍无推理落脚点
- **推理势能累积**：深层推理落脚点能否在当前世界图景内重新发现，而非必须回溯到三拍前的细节？
- **万能钥匙免疫**：任何单一变量如果过早猜中，无法跳过中间拍。验证：假设Q3猜中汤底核心变量，其余事实仍然无法自洽

---

## 闸门执行顺序

```
G10（元闸门·解题链卡完整性）
  → G1a（表层自洽）
    → G1b（真相自洽）
      → G1c（承重覆盖·反向无孤儿）
        → G2（载体约束点双模型审计）
          → G3+G4（变格指纹，本格跳过）
            → G5（情感落点）
              → G8（全量双模型审计·按载体分流）
                → G6（子代理盲测）
                  → G7（标准清单穿透）
                    → G9（模拟问答·对照解题链卡）
                      → G11（推理保证·匿名模拟）
```

**任何改动后**：先按 `appendix/e-repair-diagnosis.md` 的”修后最小复跑”矩阵确定范围。改动锚点、接缝、拍间入口、状态协议或核心机制时，不能只重跑 G1a→G1c→G2→G8。改动涉及信念更新逻辑时必须重跑 G11。

---

## 难度校准总表

| 闸门 | E | D | C | B | A+ |
|------|---|---|---|---|---|
| **G6·q1命中方向** | 允许 | **不允许** | 不允许 | 不允许 | 不允许 |
| **G6·前5问命中方向** | 允许 | 允许 | **不允许** | 不允许 | 不允许 |
| **G6·前10问命中方向** | 允许 | 允许 | 允许 | **不允许** | 不允许 |
| **G6·前10问命中公理** | 允许 | 允许 | **不允许** | 不允许 | 不允许 |
| **G6·前15问命中公理** | 允许 | 允许 | 允许 | 允许 | **不允许** |
| **G7·标准清单前N问穿透** | ≤3问 | ≤5问 | **≤8问** | ≤12问 | ≤15问 |
| **G11·穷举点容忍** | ≤2个 | ≤1个 | **0个** | 0个 | 0个 |
| **G11·独立证据族数** | ≥1 | ≥1 | ≥2 | ≥2 | ≥3 |
| **G11·种子质量（多拍）** | 弱种子可 | 弱种子可 | **强种子** | 强种子 | 强种子 |
| **G1c·变量L1覆盖率** | ≥80% | ≥90% | **100%** | 100% | 100% |
